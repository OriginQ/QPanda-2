cmake_minimum_required(VERSION 3.1)
project(Grover_Algorithm)
file(GLOB_RECURSE GROVER_CPP ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp )
add_executable(${PROJECT_NAME} ${GROVER_CPP})
target_link_libraries(${PROJECT_NAME} ${LIB_QALG} ${LIB_QPANDA} )

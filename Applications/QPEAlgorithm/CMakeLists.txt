cmake_minimum_required(VERSION 3.1)
project(QPE_Algorithm)
file(GLOB_RECURSE QPE_CPP ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp )
add_executable(${PROJECT_NAME} ${QPE_CPP})
target_link_libraries(${PROJECT_NAME} ${LIB_QPANDA})

cmake_minimum_required(VERSION 3.1)
project(DJ_Algorithm)
file(GLOB_RECURSE DJ_CPP ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp )
add_executable(${PROJECT_NAME} ${DJ_CPP})
target_link_libraries(${PROJECT_NAME} ${LIB_QALG})

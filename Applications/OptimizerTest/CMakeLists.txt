cmake_minimum_required(VERSION 3.1)
project(OptimizerTest)
file(GLOB_RECURSE OP_CPP ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp )
add_executable(${PROJECT_NAME} ${OP_CPP})
target_link_libraries(${PROJECT_NAME} ${LIB_QALG} ${LIB_COMPONENTS})
